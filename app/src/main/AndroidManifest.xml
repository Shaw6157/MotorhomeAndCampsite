<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ais.mnc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.activity.SplashActivity">
        </activity>
        <activity android:name=".view.activity.UserLoginActivity" />
        <activity android:name=".view.activity.UserSignUpActivity" />
        <activity
            android:name=".view.activity.CampsiteListActivity"
            android:label="@string/clist_title"
            android:theme="@style/AppTheme" />
        <activity android:name=".view.activity.CampsiteDetailActivity" />
        <activity android:name=".view.activity.CampsiteMapActivity" />
        <activity android:name=".view.activity.PhotoListActivity" />
        <activity android:name=".view.activity.PhotoDetailActivity" />
        <activity android:name=".view.activity.VehicleTypeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.VehicleDetailActivity" />
        <activity android:name=".view.activity.OrderListActivity" />
        <activity android:name=".view.activity.OrderDetailActivity" />
        <activity android:name=".view.activity.VehicleListActivity"></activity>
    </application>

</manifest>